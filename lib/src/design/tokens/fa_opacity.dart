/// The MT 2.0 opacity scale.
///
/// Not a `ThemeExtension` — see [FaSpace] for why.
abstract final class FaOpacity {
  /// Something shown but not available yet, such as a locked activity card.
  static const double disabled = 0.6;
}
